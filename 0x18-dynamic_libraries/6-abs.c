#include "main.h"

/**
 * _abs - conpute the absolute value of an integer
 * @r: the integer to be computed
 * Return: absolute value
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);
}

